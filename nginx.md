# nginx HTTP server

### persiapan

- unduh `nginx.zip` dari https://nginx.org/en/download.html
- lalu ekstrak ke dalam folder `./nginx/`

### eksekusi

```cmd
cd C:\waap\nginx\

.\nginx.exe -v
.\nginx.exe

.\nginx.exe -s stop
.\nginx.exe -s quit
```
